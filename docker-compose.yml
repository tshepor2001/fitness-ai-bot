services:
  api:
    build: .
    container_name: fitness-ai-bot
    restart: unless-stopped
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - bot-data:/app/data

volumes:
  bot-data:
